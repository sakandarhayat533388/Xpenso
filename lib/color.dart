import 'package:flutter/material.dart';

// --- Primary Gradient (AppBar, Hero Card) ---
const Color primaryStart = Color(0xFF1ABC9C); // Teal Green
const Color primaryEnd   = Color(0xFF3498DB); // Soft Blue

// --- Background ---
const Color backgroundColor = Color(0xFFF6F8FA);
const Color cardBackground  = Color(0xFFFFFFFF);

// --- Text Colors ---
const Color textPrimary   = Color(0xFF2C3E50); // Dark text
const Color textSecondary = Color(0xFF7F8C8D); // Gray text
const Color textWhite     = Colors.white;

// --- Status Colors ---
const Color expenseColor = Color(0xFFE74C3C); // Red for expense
const Color incomeColor  = Color(0xFF2ECC71); // Green for income
